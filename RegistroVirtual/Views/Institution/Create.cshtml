@model Models.InstitutionModel

@{
    ViewBag.Title = "Institución";
    Layout = "~/Layouts/DashboardLayout.cshtml";
}

<div id="page-wrapper">
    <div class="container-fluid">

        <!-- Page Heading -->
        <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header">
                    Institución
                </h1>
            </div>
        </div>

        @if (!string.IsNullOrEmpty(ViewBag.ErrorMessage))
        {
            <!-- Error -->
            <div class="row">
                <div class="col-lg-6 error-message">
                    @ViewBag.ErrorMessage
                </div>
            </div>
        }

        <!-- Form -->
        <div class="col-md-4">
            @using (Html.BeginForm("Save", "Institution", FormMethod.Post))
            {
                @Html.HiddenFor(m => m.Id)

                <div class="form-group">
                    <label>Nombre:</label>
                    @Html.TextBoxFor(m => m.Name, new { required = "required", placeholder = "Nombre", @class = "form-control" })
                </div>

                <button type="submit" class="btn btn-primary btn-block btn-large">Guardar</button>
            }
        </div>
    </div>
</div>



