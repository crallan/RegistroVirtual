@model Models.ImportModel

@{
    ViewBag.Title = "Importar";
    Layout = "~/Layouts/DashboardLayout.cshtml";
}

<link href="~/Content/css/site/pages/import.css" rel="stylesheet" />

<div id="page-wrapper">
    <div class="container-fluid">

        <!-- Page Heading -->
        <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header">
                    Importar Estudiantes
                </h1>
            </div>
        </div>

        @if (!string.IsNullOrEmpty(ViewBag.SuccessMessage))
        {
            <!-- Error -->
            <div class="row">
                <div class="col-lg-6 success-message">
                    @ViewBag.SuccessMessage
                </div>
            </div>
        }


        @if (!string.IsNullOrEmpty(ViewBag.ErrorMessage))
        {
            <!-- Error -->
            <div class="row">
                <div class="col-lg-6 error-message">
                    @ViewBag.ErrorMessage
                </div>
            </div>
        }

        <!-- Instructions -->
        <div class="col-md-6 import-instructions">
           <p>
               En el siguiente link podrá encontrar una plantilla con la estructura necesaria para poder realizar la importación
               de estudiantes de manera correcta. El archivo cuenta con las columnas de cédula, nombre, primer apellido, segundo apellido 
               y sección. Este proceso de importacíon generará la sección con el nombre definido en la respectiva columna y asignará los estudiantes a 
               dicha sección.

               El proceso permite importar multiples secciones en un solo archivo, cada seccion debe estar un una hoja del archivo de manera separada.
           </p>
           <button type="button" class="btn btn-primary btn-block btn-large" id="download-template">Descargar Plantilla</button>
        </div>

        <!-- Form -->
        <div class="col-md-4 import-form">
            @using (Html.BeginForm("Import", "Student", FormMethod.Post, new { enctype = "multipart/form-data" }))
            {
                <div class="form-group">
                    <label>Importar Archivo:</label>
                    <input type="file" name="importFile"/>
                </div>
                <button type="submit" class="btn btn-primary btn-block btn-large">Importar</button>
            }
        </div>
    </div>
</div>

<script src="~/Content/js/site/import.js"></script>