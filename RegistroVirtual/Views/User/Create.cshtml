@model Models.UserModel

@{
    ViewBag.Title = "Perfil de Usuario";
    Layout = "~/Layouts/DashboardLayout.cshtml";
}

<link href="~/Content/css/chosen/chosen.min.css" rel="stylesheet" />

<div id="page-wrapper">
    <div class="container-fluid">

        <!-- Page Heading -->
        <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header">
                    Perfil de Usuario 
                </h1>
            </div>
        </div>

        @if (!string.IsNullOrEmpty(ViewBag.ErrorMessage))
        {
            <!-- Error -->
            <div class="row">
                <div class="col-lg-6 error-message">
                    @ViewBag.ErrorMessage
                </div>
            </div>
        }

        <!-- Form -->
        <div class="col-md-4">
            @using (Html.BeginForm("Save", "User", FormMethod.Post))
            {
                @Html.HiddenFor(m => m.Id)

                <div class="form-group">
                    <label>First Name:</label>
                    @Html.TextBoxFor(m => m.FirstName, new { required = "required", placeholder = "First Name", @class = "form-control" })
                </div>
                <div class="form-group">
                    <label>Last Name:</label>
                    @Html.TextBoxFor(m => m.LastName, new { required = "required", placeholder = "Last Name", @class = "form-control" })
                </div>
                <div class="form-group">
                    <label>User Name:</label>
                    @Html.TextBoxFor(m => m.Username, new { required = "required", placeholder = "User Name", @class = "form-control" })
                </div>
                <div class="form-group">
                    <label>Password:</label>
                    @Html.PasswordFor(m => m.Password, new { required = "required", @class = "form-control", value = Model.Password })
                </div>
                <div class="form-group">
                    <label>Confirm Password:</label>
                    @Html.PasswordFor(m => m.ConfirmPassword, new { required = "required", @class = "form-control", value = Model.Password })
                </div>
                <div class="form-group">
                    <label>Subjects:</label>
                    @Html.ListBoxFor(m => m.SelectedSubjects, Model.Subjects, new { @class = "form-control chzn-select", data_placeholder = "Asignaturas..." })
                </div>

                <button type="submit" class="btn btn-primary btn-block btn-large">Save</button>
            }
        </div>
    </div>
</div>

<script src="~/Content/js/plugins/chosen/chosen.jquery.min.js"></script>
<script src="~/Content/js/site/create-user.js"></script>